name: sana3i_kuwait
description: الحرفي الكويتي - Kuwaiti Craftsman Marketplace
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  easy_localization: ^3.0.5
  # ✅ Firebase packages removed — migrated to PostgreSQL/Prisma via Next.js API
  # firebase_core: ^3.4.0
  # firebase_auth: ^5.2.0
  # cloud_firestore: ^5.4.0
  # firebase_crashlytics: ^4.1.0
  # firebase_messaging: ^15.1.0
  # firebase_storage: ^12.3.0
  app_links: ^6.1.1
  http: ^1.2.0
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.2.2
  url_launcher: ^6.2.4
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1
  intl: ^0.20.2
  flutter_local_notifications: ^17.1.0
  geolocator: ^12.0.0
  google_maps_flutter: ^2.6.1
  socket_io_client: ^2.0.3+1
  flutter_rating_bar: ^4.0.1
  shimmer: ^3.0.0
  photo_view: ^0.15.0
  webview_flutter: ^4.7.0
  path_provider: ^2.1.3
  pdf: ^3.11.1
  printing: ^5.13.2
  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/translations/
    - assets/images/
