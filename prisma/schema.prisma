generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model PaymentTransaction {
  id          Int       @id @default(autoincrement())
  paymentId   String
  invoiceId   String?
  requestId   Int
  type        String
  amount      Float
  status      String
  paymentUrl  String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  request     Request   @relation(fields: [requestId], references: [id])
}

model Request {
  id            Int       @id @default(autoincrement())
  status        String
  visitFeePaid  Boolean   @default(false)
  paymentStatus String?
  clientId      Int?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  transactions  PaymentTransaction[]
  // ... باقي حقول الجدول الموجودة لديك مسبقاً
}

// ️ مهم: تأكد من إضافة باقي الموديلات (User, Category, etc.) 
// التي كانت موجودة في ملفك الأصلي هنا، لأن هذا الأمر سيستبدل الملف بالكامل.
